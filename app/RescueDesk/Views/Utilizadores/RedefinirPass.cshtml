@using RescueDesk.ViewModels
@model ConfirmRegistoViewModel
@{
    ViewBag.Title = "Recuperar Palavra-Passe";
    Layout = "~/Views/Shared/BackOffice/_Login.cshtml";
}

<div class="login-content">
    <div class="login-form">
        <div class="login-logo">
            <a href="@Url.Action("Index", "HomePage")">
                <img class="align-content" src="/images/logo_shadow.png" alt="">
            </a>
        </div>
        <hr style="width:80%" />
        @using (Html.BeginForm())
        {
            @Html.ValidationSummary(true)
            <div class="form-group">
                @Html.LabelFor(model => model.Username, new { htmlAttributes = new { @class = "form-control" } })
                @Html.EditorFor(model => model.Username, new { htmlAttributes = new { @class = "form-control", @placeholder = "Introduza o seu email..." } })
            </div>
            <button style="border-radius:2px" type="submit" class="btn btn-primary btn-flat m-b-30 m-t-30">Enviar Email</button>
            <div class="register-link m-t-15 text-center">
                <p style="margin-top: 10px;margin-bottom: 12px">Já tem uma conta? <a href="@Url.Action("Login")">Entre aqui!</a></p>
            </div>
        }
    </div>
</div>