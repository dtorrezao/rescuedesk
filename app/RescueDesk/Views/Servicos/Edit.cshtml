@model RescueDesk.Models.Atividade

@{
    Layout = null;
    ViewBag.Title = "Serviço";
    ViewBag.SubTitle = "Editar Serviço";
}

@using (Html.BeginForm())
{
    if (!string.IsNullOrEmpty((string)ViewBag.SubTitle))
    {
        <div class="modal-header">
            <h5 class="modal-title" id="staticModalLabel">@ViewBag.SubTitle</h5>
            <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                <span aria-hidden="true">&times;</span>
            </button>
        </div>
    }
    <div class="modal-body">
        @Html.AntiForgeryToken()
        @Html.HiddenFor(model => model.idatividade)
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="row">
            <div class="col col-md-2" style="margin-right: -44px; margin-left: 4px">
                <label for="@Html.NameFor(model=>model.descricao)" class=" form-control-label" style="line-height: 38px">@Html.DisplayNameFor(model => model.descricao):</label>
            </div>
            <div class="col col-md-7">
                @Html.EditorFor(model => model.descricao, new { htmlAttributes = new { @class = "form-control", @placeholder = "Introduza uma breve descrição do serviço..." } })
                @Html.ValidationMessageFor(model => model.descricao, "", new { @class = "text-danger" })
            </div>
            <div class="col col-md-1" style="margin-right: -12px; margin-left: -8px">
                <label for="@Html.NameFor(model=>model.peso)" class=" form-control-label" style="line-height: 38px">@Html.DisplayNameFor(model => model.peso):</label>
            </div>
            <div class="col col-md-2" style="margin-left: 2px;">
                @Html.EditorFor(model => model.peso, new { htmlAttributes = new { @class = "form-control", @style = "width: 160px", @placeholder = "do serviço..." } })
                @Html.ValidationMessageFor(model => model.peso, "", new { @class = "text-danger" })
            </div>
        </div>

    </div>
    <div class="modal-footer">
        <button type="submit" class="btn btn-primary"><i class="fa fa-floppy-o"></i>&nbsp; Guardar</button>
        <button type="button" class="btn btn-secondary" data-dismiss="modal"> <i class="fa fa-times"></i>&nbsp;Cancelar</button>
    </div>
}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
